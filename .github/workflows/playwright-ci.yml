name: Playwright E2E Performance Tests

on:
  push:
    branches:
      - '**'
  pull_request:
    branches:
      - '**'
  workflow_dispatch:
    inputs:
      duration:
        description: 'Test run duration in minutes (e.g. 30)'
        required: false
        default: '30'
      headless:
        description: 'Run headless (true = headless, false = headed)'
        required: false
        default: 'true'
      base_url:
        description: 'Base URL of the React app under test'
        required: false
        default: 'https://qa-react.ecolane.com/drt'


env:
  NODE_VERSION: '20'
  HEADLESS: 'true'
  BASE_URL: ${{ github.event.inputs.base_url || vars.BASE_URL || 'https://qa-react.ecolane.com/drt' }}

jobs:
  playwright-tests:
    name: Run Playwright Performance Tests
    runs-on: ubuntu-latest
    timeout-minutes: 180

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install npm dependencies
        run: npm ci

      - name: Install Playwright Chromium browser and OS dependencies
        run: npx playwright install chromium --with-deps

      - name: Run duration-based performance tests
        run: node scripts/run-duration.mjs --duration ${{ github.event.inputs.duration || '30' }} --spec tests/react-booking-new.spec.ts
        env:
          CI: 'true'
          HEADLESS: ${{ github.event.inputs.headless || 'true' }}
          BASE_URL: ${{ env.BASE_URL }}

      - name: Generate performance HTML report
        if: always()
        run: npm run perf:report
        env:
          CI: 'true'

      - name: Upload performance HTML report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: perf-report-${{ github.run_number }}
          path: perf-results/report.html
          retention-days: 30

      - name: Upload performance results (JSON + full folder)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: perf-results-${{ github.run_number }}
          path: perf-results/
          retention-days: 30

      - name: Upload Playwright HTML report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report-${{ github.run_number }}
          path: playwright-report/
          retention-days: 30

      - name: Upload test results (JSON, traces, screenshots)
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-results-${{ github.run_number }}
          path: test-results/
          retention-days: 30

      - name: Write job summary
        if: always()
        run: |
          echo "## Playwright E2E Performance Test Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Property | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|----------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Run Number | ${{ github.run_number }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Triggered By | ${{ github.event_name }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Branch | ${{ github.ref_name }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Commit SHA | ${{ github.sha }} |" >> $GITHUB_STEP_SUMMARY
          echo "| BASE_URL | ${{ env.BASE_URL }} |" >> $GITHUB_STEP_SUMMARY
          echo "| HEADLESS | ${{ github.event.inputs.headless || 'true' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Duration (mins) | ${{ github.event.inputs.duration || '30' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Spec | tests/react-booking-new.spec.ts |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Artifacts: perf-report, perf-results, playwright-report, test-results" >> $GITHUB_STEP_SUMMARY
